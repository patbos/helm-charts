apiVersion: v1
kind: Service
metadata:
  name: {{ include "dump1090-exporter.fullname" . }}
  labels:
    {{- include "dump1090-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "dump1090-exporter.selectorLabels" . | nindent 4 }}
